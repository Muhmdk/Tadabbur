# Top-level shared example. Each side also has its own .env.example.
# Copy to .env and fill from 1Password. Never commit real values.

# Used by docker-compose and any orchestration scripts.
REDIS_URL=redis://localhost:6379/0
